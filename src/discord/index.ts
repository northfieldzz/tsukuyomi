// import {Events, ShardingManager} from "discord.js";
//
// const manager = new ShardingManager('./', {token: ''})
// manager.on(Events.ShardReady., shard => console.info(`Launched shard ${shard.id}`))
// manager.spawn()
//
